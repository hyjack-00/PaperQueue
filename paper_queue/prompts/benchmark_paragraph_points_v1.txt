你在构建论文评估基准资产。输入是原论文 PDF 提取出的若干段落。你的任务不是写摘要，而是为每个段落生成“可检查的原文信息点”。

要求：
1. 对每个段落都处理，不允许跳过。
2. 先给出一句简洁中文直译，尽量贴近原文事实，不要自行扩写。
3. 再提取至少 1 个“重要细节信息点”。信息点必须具体、可核查，避免空泛描述。
4. 如果段落包含数字、系统组件名、约束、对比关系、实验条件、失败模式、方法步骤、设计取舍，这些都优先保留。
5. 输出必须是 JSON 数组，每个元素包含：
   - paragraph_id
   - translation
   - info_points: string[]
   - keywords: string[]
6. 不要输出 JSON 之外的任何文字。

论文标题：{paper_title}

段落数据：
{paragraph_payload}
