请基于论文内容生成一份精炼但完整的中文阅读稿，直接输出 markdown 正文，不要输出 YAML frontmatter，不要输出前言。

请使用统一结构，但不要把格式写得过碎。严格采用下面的顶层结构：

## TL;DR
3 到 5 条短 bullet，每条突出一个 paper-specific 结论。

## 论文基本信息
只保留 Title、Venue、Year、Authors、Institution、Paper URL、Framework Version 这几个字段；不要写 repo、notebook 等噪音字段。

## 1. 整体概括
## 2. 背景与动机 (Background & Motivation)
## 3. 方法与系统设计 (Method & System Design)
## 4. 实验设置 (Evaluation Setup)
## 5. 结果与分析 (Results & Analysis)
## 6. 总结与思考 (Conclusion)

各章节可以用短段落加 bullet 混排，但不要继续拆太多子标题。

要求：
1. 内容要覆盖完整，不能因为格式精炼而遗漏关键方法、实验或结论。
2. 不要编造论文中没有出现的信息，不确定就明确写"论文未明确说明"。
3. 保留关键英文术语，采用"中文（English）"或直接使用英文术语的方式。以下英文术语必须至少出现一次：
   - 背景/动机段：问题定义、研究动机相关的核心术语
   - 方法段：论文提出的核心方法、系统架构、技术方案的关键术语
   - 实验设置段：必须包含 "evaluation setup" 或等价的英文术语，描述实验环境、数据集、基准配置
   - 结果段：必须包含 "evaluation result" 或 "findings" 等英文术语，并写出具体数字和对比
   - 总结段：必须包含 "deployment risk" 或 "deployment implication" 或 "open problem" 等英文术语，讨论局限性、部署风险或开放问题
4. 不要输出类似 [1]、[2] 的引用标号。
5. 结果与分析必须写出具体数字、对比对象和工程意义。
6. 总结与思考必须写局限性、部署风险或开放问题，不能只是重复摘要。
7. 按论文原文结构理解内容，尤其要区分 background/motivation、method/system design、experimental setup、results。
8. 这篇论文的通用类型偏向 {archetype}，请遵循这个类型的常见分析重点：{archetype_requirement}
9. 这篇论文的结构篇幅建议如下，请据此动态分配篇幅，而不是平均分配：
{section_guidance}
10. 不要把正文压缩成只有高层结论。背景、方法、实验、结果各部分都要保留原文中的关键细节信息点，尤其是：
   - 背景与动机：问题定义、静态方法的局限、关键 trade-off
   - 方法与系统设计：架构组件、workflow 步骤、evaluator / implementation / deployment 机制
   - 实验设置：场景、指标、基线、环境
   - 结果与分析：按场景拆分结果，不要只给总提升数字
11. 如果原论文某一节本身篇幅很长，你的对应章节也必须相对更长；不要把长方法论文压成短方法段。
12. 对包含 figure 的核心内容，正文必须在对应段落里直接解释该图支撑的论点，而不是只给图和一句 caption。
术语规范：
- 涉及真实环境/真实场景时，使用 "real-world environment（真实环境）" 或 "realistic environment"
- 涉及反馈循环/迭代优化时，使用 "feedback loop（反馈循环）"
- 涉及搜索/优化策略时，使用 "search strategy（搜索策略）"
- 涉及模块化设计时，使用 "modular（模块化）"
- 涉及安全风险/滥用时，使用 "safety risk" 或 "misuse（滥用）"
- 涉及自主智能体时，使用 "autonomous agents（自主智能体）"
- 涉及大规模服务时，使用 "serving at scale（大规模服务）"
- 涉及并行策略时，使用 "parallelism strategy（并行策略）"
